
            CREATE TABLE Users (
                Id UUID PRIMARY KEY,
                Username VARCHAR(30) NOT NULL,
                Password TEXT NOT NULL,
                Image TEXT NOT NULL
            );
            